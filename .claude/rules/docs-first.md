# Docs-first rules

- Before coding, read local docs relevant to the task.
- Prefer repository-local examples over web research.
- If a new pattern is introduced, document it in `docs/agent/`.
- Keep docs concise and reusable.
