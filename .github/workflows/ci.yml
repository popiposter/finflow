name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  backend-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI bootstrap note
        run: |
          echo "Backend code is not scaffolded yet."
          echo "Replace this placeholder with lint/test jobs once backend pyproject and app are added."
